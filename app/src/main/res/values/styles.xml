<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/btnDefColor</item>
        <item name="colorPrimaryDark">@color/btnDefColorDark</item>
        <item name="colorAccent">@color/textDefColor</item>
    </style>

</resources>
